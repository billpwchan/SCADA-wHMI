import { CardsToCsvPipe } from './cards-to-csv.pipe';

describe('CardsToCsvPipe', () => {
  it('create an instance', () => {
    const pipe = new CardsToCsvPipe();
    expect(pipe).toBeTruthy();
  });
});
